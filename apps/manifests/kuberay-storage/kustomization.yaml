apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kuberay-system

resources:
# Option 1: Shared storage (requires NFS or Longhorn)
- ray-models-shared-pvc.yaml

# Option 2: Per-node storage (works with local-path, but wasteful)
# Uncomment if using local-path without NFS:
# - model-storage-pvcs.yaml

commonLabels:
  app: ray-storage
  managed-by: argocd
